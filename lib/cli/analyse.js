'use strict'

const Logger = require('../util/logger')

class Analyse {
  constructor (config) {
    this.config = config
  }

  async run () {
    Logger.global.info(`TODO`)
  }
}

exports = module.exports = Analyse
